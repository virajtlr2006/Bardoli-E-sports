export const OWNER_EMAIL = (process.env.OWNER_EMAIL || "vaibhav@gmail.com")
	.toString()
	.trim()
	.toLowerCase();

export const OWNER_PASSWORD = (process.env.OWNER_PASSWORD || "vaibhav@10")
	.toString()
	.trim();
